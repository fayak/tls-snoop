# TLS Snoop configuration
# This file is sourced by the systemd service

# Command-line options for tls-snoop
# See 'tls-snoop --help' for all options
#
# Note: Log directory /var/log/tls-snoop is managed by systemd
#
# Examples:
#   Listen on all interfaces, default port 443 (daemon mode):
#     TLS_SNOOP_OPTS="--quiet --json /var/log/tls-snoop/tls-snoop.log"
#
#   Listen on specific interface:
#     TLS_SNOOP_OPTS="--quiet eth0 --json /var/log/tls-snoop/tls-snoop.log"
#
#   Monitor multiple ports:
#     TLS_SNOOP_OPTS="--quiet --port 443 --port 8443 --json /var/log/tls-snoop/tls-snoop.log"
#
#   Enable Prometheus metrics:
#     TLS_SNOOP_OPTS="--quiet --metrics --json /var/log/tls-snoop/tls-snoop.log"

TLS_SNOOP_OPTS="--quiet --json /var/log/tls-snoop/tls-snoop.log"
